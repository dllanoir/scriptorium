---
wave: 1
depends_on: []
files_modified: ["package.json", "playwright.config.js", "tests/auth.spec.js", "tests/crud.spec.js"]
autonomous: true
---

# Plan: Phase 1 - E2E Testing Foundation

## Objective
Set up Playwright and implement End-to-End tests for Authentication and CRUD operations to provide a safety net for future refactoring.

## Tasks

<task>
<id>1</id>
<title>Setup Playwright</title>
<description>Initialize package.json and install Playwright for End-to-End testing.</description>
<read_first>
- c:\Users\diogo\scriptorium\scriptorium\.planning\REQUIREMENTS.md
</read_first>
<action>
1. Run `npm init -y` in the root directory to create `package.json`.
2. Run `npm init playwright@latest --yes -- --quiet --browser=chromium` to scaffold Playwright.
3. Update `playwright.config.js` to set the `baseURL` to `http://127.0.0.1:5500` (or the preferred local dev server).
4. Remove the example tests generated by the scaffolding.
</action>
<acceptance_criteria>
- `package.json` exists and contains `@playwright/test` in `devDependencies`.
- `playwright.config.js` is present in the root directory.
- `npx playwright test` can run without errors (even if no tests exist yet).
</acceptance_criteria>
</task>

<task>
<id>2</id>
<title>Implement Authentication Tests</title>
<description>Write E2E tests for the Login and Logout flows.</description>
<read_first>
- c:\Users\diogo\scriptorium\scriptorium\src\app.js
- c:\Users\diogo\scriptorium\scriptorium\src\api\index.js
</read_first>
<action>
1. Create `tests/auth.spec.js`.
2. Write a test case that navigates to the app, inputs test credentials into the login form (`DOM.landingLoginForm`), and asserts that the user is successfully logged in (e.g., UI updates to show collections).
3. Write a test case for logging out (clicking `#auth-button`).
</action>
<acceptance_criteria>
- `tests/auth.spec.js` contains a test for login.
- `tests/auth.spec.js` contains a test for logout.
</acceptance_criteria>
</task>

<task>
<id>3</id>
<title>Implement CRUD Tests</title>
<description>Write E2E tests for Collections and Texts operations.</description>
<read_first>
- c:\Users\diogo\scriptorium\scriptorium\src\app.js
- c:\Users\diogo\scriptorium\scriptorium\src\core\controllers.js
</read_first>
<action>
1. Create `tests/crud.spec.js`.
2. Write a test for creating a new collection.
3. Write a test for creating a new text note within a collection.
4. Write a test for editing an existing text note.
</action>
<acceptance_criteria>
- `tests/crud.spec.js` contains tests for creating a collection, creating a note, and editing a note.
</acceptance_criteria>
</task>

## Verification
- Run `npx playwright test` and ensure all tests pass.
- Verify `package.json` and testing files do not interfere with the ability to open `index.html` directly in the browser.

## Must-Haves
- Playwright is installed and configured.
- Authentication tests are passing.
- CRUD tests for Collections and Texts are passing.
